import { FilterParentsPipe } from './filter-parents.pipe';

describe('FilterParentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterParentsPipe();
    expect(pipe).toBeTruthy();
  });
});
