import { FilterProfPipe } from './filter-prof.pipe';

describe('FilterProfPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProfPipe();
    expect(pipe).toBeTruthy();
  });
});
